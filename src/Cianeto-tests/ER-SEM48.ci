@cep(3, "Os nomes dos tipos besicos são reservados e não podem ser nomes de classes",
    "Identifier expected", "comp.Comp.classDec()")

class Int { //#
}



class Program 
  
    func run { } 

end
