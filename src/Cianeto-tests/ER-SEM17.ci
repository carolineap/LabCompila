@cep(7, "readInt nao toma parametros", 
   "Command 'read' expects a variable", "comp.Compiler.readStatement())")
  
      
class Program 
  
    func run {
        Out.println: (In.readInt: 0);
    }

end

