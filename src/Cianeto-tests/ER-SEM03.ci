@cep(7, "Redeclaração da varievel  'i'", "Variable 'i' is being redeclared",
  "comp.Compiler.localDec()")
  
class Program 
  
    func run {
       var Int i;
       var Int i;   //#
       
       i = In.readInt;
       Out.println: i;
    }

end
