@cep(10, "Menos unario aplicado a booleano", 
  "Operator '-' does not accepts 'boolean' expressions",
  "comp.Compiler.signalFactor()")
  

class Program 
   
   func run {
      var Boolean b;
      b = false;
      Out.println: -b;  //#
   }   

end
