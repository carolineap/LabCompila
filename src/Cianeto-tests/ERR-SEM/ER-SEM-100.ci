/*
Vitor Mesquita Fogaça
vitor.fogaca@outlook.com
*/
      
@cep(21, "variable 'erro' not found", "variable 'erro' not found", "")

class Teste
    var Int n;
	
    func get -> Int {
        return self.n;
    }
	
    func set: Int n {
        self.n = n;
    }
end


class Program
    func run {
        var Teste t;
        var Int a;
        t = Teste.new;
        a = In.readInt;
        t.set: erro;
        Out.println: t.get;
    }
end
