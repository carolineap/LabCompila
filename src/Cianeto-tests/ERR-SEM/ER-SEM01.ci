@cep(16, "Método com tipo de retorno deve ter pelo menos um comando return",
  "Missing 'return' statement in method 'm'", 
  "comp.Compiler.methodDec()" )


class Program 

  func run -> Int {

  	var Int a;

    if (2 > 8) {
      a = 2;
    }

    return 1;

   }



    private func doce: Int acucar, Boolean valor -> Int {
		return 8;
	}

  

end

open 
class Alimento
	
	private func doce: Int acucar, Boolean valor -> Int {
		return 8;
	}

end


open
class Fruta extends Alimento
	
	var Boolean a;
	var Boolean v;

	func receita -> Boolean {
		return false;
	} 

end

class Amora extends Fruta

	func teste {

		var Boolean a;
		var Fruta f, s;

		a = super.v;

		f = Alimento.new;

		s = f;


	}

end