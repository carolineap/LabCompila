/* 
Isabela Salmeron Boschi
isabelasalmeron@msn.com
*/
@cep(7, "expressao ausente depois da ','", "Expression expected", "comp.Compiler.factor()")
      
class Program

    func run {
      
        var Int i, j;
        var Int a = 0;
        var Int b = 7;

        while (a < b) {
        	a = a + 1;
        	if (b > 5) {
        		break;
        	}
        }
       
    }

    func teste {

    	var String a = "Amora";
    	var String b = "Abacaxi";

    	Out.print: (a + b);

    	return(21 + 21);

    	;

    }

    func f {

    	repeat {

    		Out.println: i;
    		i = i + 1;

    	} until (i < 6);

    	assert 1 + 3 * 8, "SOCORRO DEUS";

    }

end